import { EnterpriseModuleDashboard } from "@/components/enterprise-ui/EnterpriseModuleDashboard";

const ValaBuilderDashboard = () => <EnterpriseModuleDashboard moduleKey="vala-builder" />;

export default ValaBuilderDashboard;
