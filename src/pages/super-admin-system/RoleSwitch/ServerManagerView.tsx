import { EnterpriseModuleDashboard } from "@/components/enterprise-ui/EnterpriseModuleDashboard";

const ServerManagerView = (_props: { activeNav?: string } = {}) => <EnterpriseModuleDashboard moduleKey="server" />;

export default ServerManagerView;
